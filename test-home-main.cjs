const assert=require('node:assert/strict');const {sets,evaluate}=require('./dist/conditions.js');let n=0;
function check(id,over,want){const s=sets.find(s=>s.id===id);const a=Object.fromEntries(s.fields.map(f=>[f.id,f.type==='bool'?f.expected:f.type==='select'?f.accept[0]:f.type==='date'?'2026-09-10':'1']));Object.assign(a,over);assert.equal(evaluate(s,a).state,want,id+JSON.stringify(over));n++;}
check('home-planned',{month:4,week:1,pharmacistWeek:40},'pass');check('home-planned',{month:5},'fail');check('home-planned',{week:2},'fail');check('home-planned',{pharmacistWeek:41},'fail');check('home-planned',{special:'special',month:8,week:2},'pass');check('home-planned',{special:'special',month:9},'fail');check('home-planned',{special:'special',week:3},'fail');check('home-planned',{benefit:'no'},'unknown');check('home-planned',{special:''},'unknown');
for(const id of ['home-urgent1','home-urgent2']){check(id,{month:4},'pass');check(id,{month:5},'fail');check(id,{special:'special',month:8},'pass');check(id,{special:'special',month:9,exception:'yes'},'pass');check(id,{special:'special',month:9,exception:'no'},'fail');check(id,{special:'special',month:9,exception:''},'unknown');check(id,{visit:'no'},'fail');check(id,{disease:'no'},'fail');}
check('home-conference',{month:2},'pass');check('home-conference',{month:3},'fail');check('home-conference',{guidanceDate:'2026-09-09'},'fail');check('home-conference',{method:'other'},'fail');check('home-conference',{method:'video',privacy:'yes'},'pass');check('home-conference',{method:'video',privacy:''},'unknown');check('home-conference',{plannedFee:'yes'},'fail');check('home-conference',{basicB:'yes'},'fail');
console.log('PASS: '+n+' home limits, exception and conference cases');
